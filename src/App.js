import React from 'react';

const routes = creatBrowserRouter(
createRoutesFromElements(
  // <Route path="/" element={<HomeLayout />}>//TODO: change code
 <Route path=""/>
)
)
function App(props) {
  return (
    <div></div>
  );
}

export default App;